name: Deploy

on:
  schedule:
    - cron: '0 8 * * *' # 17:00(毎日) 日本時間は-9h
  workflow_dispatch:

jobs:
  curl:
    name: Kick deploy hook
    runs-on: ubuntu-latest
    steps:
      - name: curl
        run: curl -X POST '${{ secrets.DEPLOY_HOOK_URL }}'
